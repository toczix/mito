/**
 * Proxy entrypoint for the legacy `analyze-biomarkers` Edge Function.
 *
 * We reuse the implementation in `../analyze-biomarkers/index.ts` so that
 * both function names stay in sync without duplicating the full source.
 */
import '../analyze-biomarkers/index.ts';

