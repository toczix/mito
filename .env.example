# Supabase Configuration
# Get these from: https://app.supabase.com → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ==========================================
# IMPORTANT: Claude API Key Setup
# ==========================================
# The Claude API key is NO LONGER stored in .env!
# It's now stored securely in Supabase secrets (server-side only).
#
# To set it up:
# 1. Install Supabase CLI: brew install supabase/tap/supabase
# 2. Login: supabase login
# 3. Link project: supabase link --project-ref your-project-ref
# 4. Set secret: supabase secrets set CLAUDE_API_KEY=sk-ant-your-key
# 5. Deploy function: supabase functions deploy analyze-biomarkers
#
# See EDGE_FUNCTION_SETUP.md for details
